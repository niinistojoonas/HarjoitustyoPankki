<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joonas.pankkisovelluharkka">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Kirjautuminen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Mainactivity"
            android:label="@string/title_activity_mainactivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".UserInfo" />
        <activity android:name=".Uusi" />
        <activity android:name=".LisaaTili" />
        <activity android:name=".Saldot" />
        <activity android:name=".CardActivity" />
        <activity android:name=".TransferActivity" />
        <activity android:name=".TilitapahtumatActivity" />
        <activity android:name=".kortillaMaksu" />
        <activity android:name=".MoreMoney" />
        <activity android:name=".PankkiController" />
        <activity android:name=".ChangePassword"></activity>
    </application>

</manifest>